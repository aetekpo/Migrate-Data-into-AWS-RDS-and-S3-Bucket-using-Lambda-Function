# Migrate-Data-into-AWS-RDS-and-S3-Bucket-using-Lambda-Function
This project consists of create a lambda function that will get trigger when data got pull from guardian news API and writes into S3 bucket and MySQL RDS.
